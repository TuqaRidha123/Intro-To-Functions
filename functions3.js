// one way of defining functions 
function add(x, y) {
    return x + y;
}

// another way of defining functions 
const add = function(x, y) {
    return x + y;
}

// add(3, 4) --> 7

var square = function(x){
    return Math.pow(x, 2);
}
square();